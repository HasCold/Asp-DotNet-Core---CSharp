namespace comparison;

class ComparisonOperator{
    // Equals() in case of reference comparison 
    // == in case of value comparison 

    // Logical Operator 
    // && AND operator
    // || OR operator
    // ! NOT operator

    // Ternary operator = ?
    // It is used to solve condition based program using single line statement.
    // Console.WriteLine(num % 2 == 0 ? "true" : "false");
    // Console.WriteLine(num % 2 == 0 ? $"{num} is even" : $"{num} is odd");

}